[package]
name = "koinon-ledger"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core ledger types, account model, and dual-token balance tracking"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
